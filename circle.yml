machine:
  services:
    - docker
      - version:1.19

dependencies:
  override:
    - sudo pip install --upgrade docker-compose

test:
  override:
    - docker version
    - docker-compose -f todos/docker-compose-test.yml run django python manage.py test
